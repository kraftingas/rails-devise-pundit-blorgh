<%= comment_counter + 1 %>. <b><%= comment.commenter %></b> (<%= comment.commenter_ip %>) at <%= comment.created_at %> 
<% if user_signed_in? %>
  <% if current_user.admin? %>
    | <%= link_to t('Edit'), edit_post_comment_path(comment.post_id, comment.id) %>
  <% end %>
<% end %>
<%# link_to t('Destroy'), comment, method: :delete, data: { confirm: t('Are_you_sure') } 
%>
<%= simple_format(comment.text) %><br>
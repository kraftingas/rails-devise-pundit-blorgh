<p>
  <%= image_tag(post.image_url, size: "250x250") %>
  <%# cl_image_tag(post.image.path, size: "250x250", cloud_name: 'dlwsdtphd') if post.image.present? %>
  <%# cl_image_tag(post.image_id, :crop => :fill, :width => 120, :height => 80) if post.image_id %>
  <%= simple_format(post.text) %>
</p>